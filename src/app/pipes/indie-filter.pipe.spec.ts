import { IndieFilterPipe } from './indie-filter.pipe';

describe('IndieFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndieFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
