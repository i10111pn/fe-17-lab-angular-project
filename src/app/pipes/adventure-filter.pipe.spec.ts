import { AdventureFilterPipe } from './adventure-filter.pipe';

describe('AdventureFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdventureFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
