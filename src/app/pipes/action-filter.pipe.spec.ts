import { ActionFilterPipe } from './action-filter.pipe';

describe('ActionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ActionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
